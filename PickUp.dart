//THis files will have function and calculations pertaining to the games
//that can be called my any of the dart files


/*


class PickUpStateful extends StatefulWidget {
  PickUpStateful({Key key}) : super(key: key);

  @override
  _PickUpStatefulState createState() => _PickUpStatefulState();
}

class _PickUpStatefulState extends State<PickUpStateful> {
  int _gamesHappeningNow = 0;

  Widget build(BuildContext context) {
    return Scaffold(
      //appBar: AppBar(
      //title: Text('Sample Code'),
      //),
      body: Center(
        child: Text('Currently $_gamesHappeningNow games happening.'),
      ),
      bottomNavigationBar: BottomAppBar(
        child: Container(
          height: 50.0,
        ),
      ),
      floatingActionButton: FancyButton(
        onPressed: () => setState(() {
              _gamesHappeningNow++;
            }),
        //tooltip: 'Increment Counter',
        //child: Icon(Icons.add),
      ),
      floatingActionButtonLocation: FloatingActionButtonLocation.centerDocked,
    );
  }
}
*/